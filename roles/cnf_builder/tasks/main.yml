- name: List all binaries
  shell: ls /usr/bin/ /usr/local/bin/ /bin/ /sbin/ /usr/sbin/ 2>/dev/null
  register: bins
  changed_when: false
  failed_when: false

- name: Show binaries
  debug:
    msg: "{{ bins.stdout_lines }}"