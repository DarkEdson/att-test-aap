- name: Check available package manager
  shell: ls /usr/bin/apt-get /usr/bin/dnf /usr/bin/yum /usr/bin/apk /sbin/apk 2>/dev/null | head -1
  register: pkg_manager
  changed_when: false
  failed_when: false

- name: Show package manager
  debug:
    msg: "{{ pkg_manager.stdout }}"